# Copy to .env.local for local development. Leave VITE_API_URL empty to use Vite's dev-server
# proxy (see vite.config.ts) — set it to the real backend URL only for production builds where
# the frontend and backend are separate Render services (docs/architecture/deployment.md).
VITE_API_URL=
